# 山支度 / YAMAJITAKU —— 产品经理(PM)交接简报

> 给新加入的「产品经理」看的项目背景。你(PM)负责**想清楚要做什么、排优先级、写清楚需求**;
> 另一个窗口的「开发」(在 Cowork 里,能直接改代码/部署/验证)负责**精确执行**。
> 你不需要写代码。你的产出是「清晰的需求」,开发会把它翻译成代码。

---

## 0. 一句话产品定位
**山支度(やまじたく / YAMAJITAKU)** 是一款面向日本登山爱好者的「登山装备准备」App:
选一座山 → 自动生成装备清单 → 管理自己的装备 → 出发前逐项打勾确认。

- 用户面向:日本登山者(**界面文案一律日文**)。
- 线上域名:`www.yamajitaku.com`(网页版)+ iOS App(App Store,已上架)。

---

## 1. 【最重要】部署模型:决定「快 or 慢、要不要苹果审核」

这是 PM 必须先搞懂的一件事,因为它决定**一个需求能多快上线**。App 分两部分:

### A. 网页部分(改动快,几分钟全员生效,免审核)
- 范围:几乎所有功能和界面——首页、装备列表、装备详情、添加装备、登录后的一切页面、算法逻辑。
- 上线方式:开发改完 → `git push` → Vercel 自动部署 → **几分钟内所有用户(含已装 App 的人)立刻看到**。
- 意义:**绝大多数需求都属于这里,能当天上线,不用等苹果。**

### B. 原生部分(改动慢,要重新打包 + 苹果审核,几天)
- 范围:很窄——App 启动画面、开屏跳转逻辑、登录页外壳、iOS 系统级设置(权限、图标、版本号)。
- 上线方式:开发改完 → 需要在 Xcode 重新打包 + 提交 App Store 审核 → **几天后 + 用户手动更新才生效**。

> **PM 排期时的判断口诀:**
> 「这个需求是改**登录后的页面/功能**吗?」→ 是 → 网页,当天能上。
> 「是改**开屏/启动/登录外壳/系统设置**吗?」→ 是 → 原生,要审核,慎重、攒着一起发。

---

## 2. 技术架构(简明,帮你理解约束,不用深究)
- 前端:Next.js(App Router)+ React,部署在 Vercel。
- App 外壳:Capacitor(把网页装进 iOS App);开屏是本地页面,秒开后跳转到线上网页。
- 数据库/登录:Supabase(存用户、装备、登山计划;负责邮箱 + Google/Apple 登录)。
- 代码仓库:GitHub。

---

## 3. 铁律(这些不能碰,PM 也别提需求碰它们)
除非用户(老板 fumi)**明确单独授权**,否则开发不会做以下事,PM 也不要下这类指令:
1. **不动 Supabase 的数据库结构 / 登录(OAuth)配置 / 线上生产配置。**
2. **不删生产数据。**
3. **不做 git 历史回滚/重置。**
4. 只往 `main` 分支推;小步提交;每次改完汇报「改了什么、验证了什么、推没推」。
5. 保持线上稳定优先;有冲突或大决策,先停下问用户。

---

## 4. 现在的项目状态(截至 2026-07-02)

### 已完成并上线(网页)
- 首页(ホーム)整体 UI 重设计:顶部绿色渐变头图 + 半环形「完成度」仪表盘(带数字从浅灰到黑的入场动画)+ 装备统计 + 最近添加装备卡片。
- 底部导航栏改成苹果「液态玻璃」质感(半透明毛玻璃 + 滑动高亮 + 点击后图标变渐变绿)。
- 首页日期、完成度显示修复;完成度回退 bug 修复。
- **官方装备库变只读**:用户不能改官方装备的信息/图片,只能读;只有用户「自己添加」的装备可编辑。
- 装备详情里删掉了没用的「素材」栏。
- 添加装备的两个 bug 修复(保存后立刻反馈、切换品类/品牌)——**已推,待用户在手机上最终验证。**
- 启动加速 + 登录稳定性:开屏秒显、后台放久了不再假掉线(这部分含**原生**改动,已随 1.0.2 打包)。

### 进行中 / 待办
- **App Store 1.0.2 提交审核**:原生的「秒开 + 不假掉线」修复要靠这个版本上线;用户正在填写审核信息、准备提交。
- 「保活 + 缓存」优化:已加健康检查接口 + Service Worker 缓存;还差用户去注册一个免费的外部定时 ping 服务(让服务器不休眠)。
- 装备图片透明化:官方装备库图片处理了一部分,剩余约 259 件待处理(用户暂时搁置,不急)。

---

## 5. 【关键】你(PM)给开发下指令的格式

开发在 Cowork 里能直接改代码,但**它不知道你和用户聊了什么**。所以你产出的每条指令要「自带上下文、目标明确、可验证」。请用下面这个模板产出,用户会原样转发给开发:

```
【需求名】一句话标题

【背景/为什么】用户想解决什么问题(1–3 句)

【具体要做什么】
- 期望的最终效果(用户视角,越具体越好;涉及界面就描述位置/颜色/间距/文案)
- 涉及哪个页面/功能(能说出「首页」「装备详情」「添加装备」这种就行)

【属于网页还是原生?】(不确定就写「让开发判断」)

【验收标准】怎样算做好了(用户能自己测的现象)

【优先级】高 / 中 / 低

【注意】任何不能碰的东西、要保留的现状
```

> 小提示给 PM:
> - 你不确定「技术上能不能做 / 是网页还是原生 / 有没有坑」→ 直接在指令里写「请开发先给方案再动手」。开发会先出方案,用户确认后再改。
> - **别替开发设计代码实现细节**(比如指定用什么函数),那是开发的活;你只要把「用户要的效果」说清楚。

---

## 6. 历史教训(帮你避免踩同样的坑)
- 用户偏好**讲大白话、简洁直接**,讨厌绕弯子和术语堆砌。PM 输出也请简明。
- 改视觉时用户对**像素/间距/颜色非常敏感**,需求尽量给出具体数值或参考图。
- 「官方装备只读、用户自建可编辑」是已定的产品原则,别提反向需求。
- 涉及**登录、启动、数据库结构**的需求风险高、且多半要走审核——PM 要给这类需求打「高风险/慎重」标签,并默认要求「先出方案」。
- 大改动**先确认「只动视觉、不动后台数据/算法/推荐逻辑」**再推——用户很在意这点。

---

## 7. 快速事实卡(可能会被问到)
- 当前 App 版本:1.0.2(等待/准备审核)。
- 网页改动上线时间:`git push` 后几分钟。
- 原生改动上线时间:提交审核后数天 + 用户更新。
- 界面语言:日文。给苹果审核员的备注:英文。
